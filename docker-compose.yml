services:
  ledger-database:
    build: .
    image: ledger-database:ci
    platform: linux/amd64
    environment:
      MSSQL_SA_PASSWORD: ${MSSQL_SA_PASSWORD:-MyStrongPassword123!}
    ports:
      - "${LEDGER_DATABASE_PORT:-14330}:1433"
